# Web Framework
fastapi==0.115.0          # Our API framework — async-first, fast, modern
uvicorn==0.30.0           # ASGI server that runs FastAPI

# Data validation
pydantic==2.7.0           # Type-safe data models for requests/responses
pydantic-settings==2.3.0  # Reads .env files into typed config objects

# Google / Gmail
google-auth==2.29.0              # Google OAuth2 core library
google-auth-oauthlib==1.2.0      # OAuth2 flow helpers
google-auth-httplib2==0.2.0      # HTTP transport for Google auth
google-api-python-client==2.128.0  # Gmail API client

# AI / LLM
google-generativeai==0.7.2   # Gemini API

# Embeddings + Vector DB
chromadb==0.5.0              # Local vector database (perfect for phase 1)
sentence-transformers==3.0.0 # Local embedding model (free, no API needed)

# Database
sqlalchemy==2.0.30     # ORM for structured data
aiosqlite==0.20.0      # Async SQLite (simple local DB for phase 1)

# Async HTTP
httpx==0.27.0          # Async HTTP client

# Utilities
python-dotenv==1.0.1   # Load .env files
python-jose==3.3.0     # JWT tokens for session management
tenacity==8.3.0        # Retry logic for API calls

#numpy
numpy==1.26.4