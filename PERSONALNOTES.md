PHASE 1: Gmail AI Assistant (Foundation)
│
├── Step 1 — Project skeleton (folders, configs, entry point) X
├── Step 2 — Settings and environment management X
├── Step 3 — Gmail OAuth (login, token storage, refresh) X
├── Step 4 — Gmail Service (fetch emails, search, parse) X
├── Step 5 — Email ingestion pipeline (chunk + embed + store) 
├── Step 6 — Vector database setup (ChromaDB)
├── Step 7 — Semantic retrieval service
├── Step 8 — RAG pipeline (retrieve + generate with Gemini)
├── Step 9 — MCP server (expose Gmail as tools)
├── Step 10 — AI Agent (orchestrate everything)
└── Step 11 — Conversational chat API (final endpoint)